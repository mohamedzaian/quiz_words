import 'package:flutter/material.dart';

class IconLock extends StatelessWidget {
  const IconLock({super.key});

  @override
  Widget build(BuildContext context) {
    return Icon(Icons.lock , size: 40,color: Colors.white,);
  }
}
